# notes to run features and unit tests locally

## To run features
install behave with:       `pip install behave`
check installation with:   `behave --version`
in `tests` directory:
run with `behave`

## To run unit tests:
in `tre-bagit-to-dri-sip/tests/units`
run with `python -m unittest discover`